#include<stdio.h>
int main()
{
    int x, y;
    printf("Enter two number :");
    scanf("%d %d",&x,&y);
    if (x>y)

    printf("\n%d is larger number than %d",x,y);
else 
    printf("\n%d is larger number than %d",y,x);
    
    
}